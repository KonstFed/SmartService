<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/dark_gray</item>
        <item name="android:color">@color/text_color</item>
    </style>
    <style name="ServiceButton">
        <item name="android:background">#33FF66</item>
        <item name="android:radius">10dp</item>
        <item name="android:src">@drawable/ic_home_black_24dp</item>
    </style>
    <style name="ExplainText">
        <item name="android:gravity">center</item>
        <item name="android:color">@color/text_color</item>
    </style>
    <style name="AdditionalFieldText">
        <item name="android:layout_marginLeft">10dp</item>
        <item name="android:padding">10dp</item>
        <item name="android:gravity">left</item>
        <item name="android:color">@color/text_color</item>
        <item name="android:textSize">20sp</item>
        <item name="android:background">@drawable/tracker_switch_drawable</item>
    </style>
    <style name="bottomNavigationButton">
        <item name="android:background">@null</item>
    </style>
    <style name="ServiceSwitch" parent="Theme.AppCompat.Light">
        <!-- активное состояние (30% прозрачности для фона) -->
        <item name="colorControlActivated">@color/red</item>
        <item name="colorControlNormal">@color/dark_gray</item>
    </style>


</resources>
